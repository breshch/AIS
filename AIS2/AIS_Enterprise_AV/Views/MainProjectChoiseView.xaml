<Window x:Class="AIS_Enterprise_AV.Views.MainProjectChoiseView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainProjectChoiseView" Height="220" Width="300"
        WindowStartupLocation="CenterScreen"
        x:Name="MainProjectChoiseWindow">
    
    <Grid Margin=" 20, 30">
        
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Button Grid.Row="0" Content="Табель" Command="{Binding MonthTimeSheetCommand}" CommandParameter="{Binding ElementName=MainProjectChoiseWindow}" />
        <Button Grid.Row="1" Content="Приход" Command="{Binding IncomingCommand}" CommandParameter="{Binding ElementName=MainProjectChoiseWindow}" Margin="0, 20, 0, 0"/>
        <Button Grid.Row="2" Content="Расход" Command="{Binding OutcomingCommand}" CommandParameter="{Binding ElementName=MainProjectChoiseWindow}" Margin="0, 20, 0, 0"/>
    </Grid>
</Window>
